##
# SABnzbd - Usenet download service https://sabnzbd.org/
# Docker Hub: https://hub.docker.com/r/linuxserver/sabnzbd
##  
services:
  sabnzbd:
    container_name: sabnzbd
    image: lscr.io/linuxserver/sabnzbd:latest

#    network_mode: 'host' # Use host networking for better performance
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Los_Angeles
    volumes:
      - ${HOST_MOUNT}/config/sabnzbd:/config
      - ${HOST_MOUNT}/backups/sabnzbd:/backups
      - ${HOST_MOUNT}/data/usenet:/data/usenet
    healthcheck:
      test: curl -sf http://localhost:8080/api?mode=version || exit 1
      start_period: 30s
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    # Capped so par2/unpack jobs can't monopolize this 4-thread host and starve Plex playback:
    deploy:
      resources:
        limits:
          cpus: '1.5'
          memory: 1024M
    networks:
      - media
      - proxied

networks:
  media:
  proxied:
    external: true
    name: proxied
